// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f510e4247f532a87ac33a4e071fb33890f0882cbf34240beee2c0d74e039488f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  extractFile,
  registerDocumentExtractor,
  registerEmbeddingBackend,
  registerOcrBackend,
  registerPostProcessor,
  registerRenderer,
  registerValidator,
  unregisterDocumentExtractor,
  unregisterEmbeddingBackend,
  unregisterPostProcessor,
  unregisterRenderer,
  unregisterValidator,
} from "kreuzberg";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("plugin_api", () => {
  it("register_document_extractor_trait_bridge: register_document_extractor: trait bridge", () => {
    registerDocumentExtractor({ extractor: { name: "test-extractor", type: "test" } });
  });
  it("register_embedding_backend_trait_bridge: register_embedding_backend: trait bridge", () => {
    registerEmbeddingBackend({ backend: { name: "test-embedding-backend", type: "test" } });
  });
  it("register_ocr_backend_trait_bridge: register_ocr_backend: trait bridge", () => {
    registerOcrBackend({ backend: { name: "test-backend", type: "test" } });
  });
  it("register_post_processor_trait_bridge: register_post_processor: trait bridge", () => {
    registerPostProcessor({ processor: { name: "test-processor", type: "test" } });
  });
  it("register_renderer_trait_bridge: register_renderer: trait bridge", () => {
    registerRenderer({ renderer: { name: "test-renderer", type: "test" } });
  });
  it("register_validator_trait_bridge: register_validator: trait bridge", () => {
    registerValidator({ validator: { name: "test-validator", type: "test" } });
  });
  it("unregister_document_extractor_after_register: unregister_document_extractor", () => {
    unregisterDocumentExtractor("test-extractor");
  });
  it("unregister_embedding_backend_after_register: unregister_embedding_backend", () => {
    unregisterEmbeddingBackend("test-embedding-backend");
  });
  it("unregister_post_processor_after_register: unregister_post_processor", () => {
    unregisterPostProcessor("test-processor");
  });
  it("unregister_renderer_after_register: unregister_renderer", () => {
    unregisterRenderer("test-renderer");
  });
  it("unregister_validator_after_register: unregister_validator", () => {
    unregisterValidator("test-validator");
  });
});
